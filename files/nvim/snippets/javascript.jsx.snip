snippet     ir
abbr        import React
options     head
        import React${1:, { PropTypes \}} from 'react'
        ${0}
# ----------------------------------------------------------------

snippet     P
abbr        { PropTypes }
options     word
        { PropTypes }${0}
# ----------------------------------------------------------------

snippet     =>
abbr        => { $ }
options     word
        => {
          ${0}
        }
# ----------------------------------------------------------------

snippet     Rf
abbr        React function component
options     head
        import React, { PropTypes } from 'react'

        function ${1}(${2}) {
          return (
            ${4}
          )
        }

        $1.propTypes = {
          ${3}
        }

        export default $1
# ----------------------------------------------------------------

snippet     ptfr
abbr        PropTypes.func.isRequired,
options     word
        PropTypes.func.isRequired,
# ----------------------------------------------------------------

snippet     ptf
abbr        PropTypes.func
options     word
        PropTypes.func,
# ----------------------------------------------------------------

snippet     ptbr
abbr        PropTypes.bool.isRequired,
options     word
        PropTypes.bool.isRequired,
# ----------------------------------------------------------------

snippet     ptb
abbr        PropTypes.bool,
options     word
        PropTypes.bool,
# ----------------------------------------------------------------

snippet     ptar
abbr        PropTypes.array.isRequired,
options     word
        PropTypes.array.isRequired,
# ----------------------------------------------------------------

snippet     pta
abbr        PropTypes.array,
options     word
        PropTypes.array,
# ----------------------------------------------------------------

snippet     ptor
abbr        PropTypes.object.isRequired,
options     word
        PropTypes.object.isRequired,
# ----------------------------------------------------------------

snippet     pto
abbr        PropTypes.object,
options     word
        PropTypes.object,
# ----------------------------------------------------------------

snippet     ptnr
abbr        PropTypes.number.isRequired,
options     word
        PropTypes.number.isRequired,
# ----------------------------------------------------------------

snippet     ptn
abbr        PropTypes.number,
options     word
        PropTypes.number,
# ----------------------------------------------------------------

snippet     ptsr
abbr        PropTypes.string.isRequired,
options     word
        PropTypes.string.isRequired,
# ----------------------------------------------------------------

snippet     pts
abbr        PropTypes.string,
options     word
        PropTypes.string,
# ----------------------------------------------------------------

snippet     i
abbr        import from
options     head
        import ${1} from ${0}
# ----------------------------------------------------------------

snippet     swr
abbr        switch in reducer
options     head
        switch (action.type) {
          case ${1}:
            return {
              ${2}
            }
          case ${3}:
            return {
              ${4}
            }
          default:
            return state
        }
# ----------------------------------------------------------------

snippet     setTimeout-function
options     head
        setTimeout(() => {
          ${0}
        }, ${1:1000})
# ----------------------------------------------------------------

snippet     Object-assign
options     word
        Object.assign({}, ${1}, ${2})
# ----------------------------------------------------------------

snippet     require
options     word
        require('${0}')
# ----------------------------------------------------------------

snippet     it
abbr        it() Mocha func
options     head
        it('${1}', () => {
          ${0}
        })
# ----------------------------------------------------------------

snippet     describe
abbr        describe() Mocha func
options     head
        describe('${1}', () => {
          ${0}
        })
# ----------------------------------------------------------------

snippet     //
abbr        jsx comment
options     word
        {/*
        ${0:TARGET}
        */}
# ----------------------------------------------------------------

snippet     render
abbr        render() method
options     head
        render() {
          ${0:TARGET}
        }
# ----------------------------------------------------------------

snippet     constructor
abbr        constructor() method
options     head
        constructor(props) {
          super(props)
          ${1}
        }
# ----------------------------------------------------------------

snippet     imc
abbr        import { connect }
options     head
        import { connect } from 'react-redux'
# ----------------------------------------------------------------

snippet     s
abbr        import s from './styles.scss'
options     head
        import s from './styles.scss'
# ----------------------------------------------------------------
