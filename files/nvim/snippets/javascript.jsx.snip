snippet     ir
abbr        import React
options     head
        import React${1:, { PropTypes, Component \}} from 'react';
        ${0}
# ----------------------------------------------------------------

snippet     =>
abbr        => { $ }
options     word
        => {
          ${0}
        }
# ----------------------------------------------------------------

snippet     Rfc
abbr        React function component
options     head
        import React, { PropTypes } from 'react';

        const ${1} = (${2}) => ${0}

        $1.propTypes = {
        };

        export default $1;
# ----------------------------------------------------------------

snippet     swr
abbr        switch in reducer
options     head
        switch (action.type) {
          case ${1}:
            return {
              ${2}
            };
          case ${3}:
            return {
              ${4}
            };
          default:
            return state;
        }
# ----------------------------------------------------------------

snippet     setTimeout-function
options     head
        setTimeout(() => {
          ${0}
        }, ${1:1000})
# ----------------------------------------------------------------

snippet     Object-assign
options     word
        Object.assign({}, ${1}, ${2});
# ----------------------------------------------------------------

snippet     require
options     word
        require('${0}');
# ----------------------------------------------------------------

snippet     it
abbr        it() Mocha func
options     head
        it('${1}', () => {
          ${0}
        });
# ----------------------------------------------------------------

snippet     describe
abbr        describe() Mocha func
options     head
        describe('${1}', () => {
          ${0}
        });
# ----------------------------------------------------------------

snippet     //
abbr        jsx comment
options     word
        {/* ${0:TARGET} */}
# ----------------------------------------------------------------

snippet     render
abbr        render() method
options     head
        render() {
          ${0:TARGET}
        }
# ----------------------------------------------------------------

snippet     constructor
abbr        constructor() method
options     head
        constructor(props) {
          super(props);
          ${1}
        }
        ${0}
# ----------------------------------------------------------------




